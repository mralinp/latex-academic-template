NAME := Academic Resume / CV
DESCRIPTION := Single-column resume with a contact-icon header, education, publications, teaching, and experience sections. Built on paracol and fontawesome5.
TAGS := resume cv academic
ENGINE := pdflatex
MAIN := main.tex
SHELL_ESCAPE := false
