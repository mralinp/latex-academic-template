NAME := IEEE Transactions
DESCRIPTION := Two-column IEEE journal/transactions paper skeleton (title, authors, abstract, index terms, sections, references) using the IEEEtran class.
TAGS := ieee journal paper transactions
ENGINE := pdflatex
MAIN := main.tex
SHELL_ESCAPE := false
